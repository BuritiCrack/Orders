@page "/states/edit/{StateId:int}"

<h3>Editar Estado/Departamento</h3>

@if(state == null)
{
    <Loading />
}
else
{
    <FormWithName @ref="stateForm" Model="state" Label="Estado/Departamento" OnValidSubmit="SaveAsync" ReturnAction="Return" />
}
